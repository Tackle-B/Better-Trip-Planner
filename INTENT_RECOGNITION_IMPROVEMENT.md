# 意图识别改进文档

## 问题描述

**原问题：** Agent会对所有用户输入都尝试生成旅行计划，即使用户只是询问简单信息（如"龙门石窟和洛邑古城距离多远？"）也会触发计划生成。

## 解决方案

### 1. 改进意图识别逻辑

**文件：** `backend/app/agents/chat_agent.py`

#### 改进前
- 使用简单的关键词匹配
- 只要包含"想去"、"旅行"等词就判定为规划意图
- 容易误判普通问答为规划请求

#### 改进后
- **使用LLM进行智能意图识别**
- 明确定义三种意图类型：
  - `plan_trip`：明确要规划旅行，必须包含目的地和天数
  - `modify_plan`：修改已有计划
  - `general_chat`：普通问答，不需要生成计划

- **判断原则：**
  - 只有用户明确表达"规划行程"、"安排旅行"且提供天数时，才判定为 `plan_trip`
  - 如果只是询问信息（距离、天气、景点、美食等），判定为 `general_chat`
  - 如果对话中已有计划且用户表达修改意图，判定为 `modify_plan`

### 2. 增强普通对话处理

**新增功能：**

#### 2.1 工具调用决策
- 自动判断用户问题是否需要调用工具
- 支持的工具类型：
  - `distance`：查询两地距离
  - `poi_search`：搜索景点、餐厅、酒店
  - `weather`：查询天气信息

#### 2.2 智能工具调用
- **距离查询：** "龙门石窟和洛邑古城距离多远？" → 调用高德地图API查询距离
- **POI搜索：** "西湖周边有什么景点？" → 调用景点搜索工具
- **天气查询：** "现在去三亚天气怎么样？" → 调用天气查询工具
- **一般问答：** "北京有什么特色？" → 使用LLM直接回答

### 3. 改进计划修改处理

**改进前：**
- 只返回固定提示文本："好的，我理解您想调整计划..."

**改进后：**
- 从对话历史中提取原有计划参数
- 使用LLM分析用户想修改什么
- 合并原参数和修改后的参数
- 重新生成计划
- 提供友好的反馈

## 测试用例

| 用户输入 | 期望意图 | 说明 |
|---------|---------|------|
| "我想去北京玩3天" | `plan_trip` | 明确的规划请求 |
| "帮我规划一下上海5日游" | `plan_trip` | 明确的规划请求 |
| "龙门石窟和洛邑古城距离多远？" | `general_chat` | 询问距离，调用工具 |
| "北京有什么好吃的？" | `general_chat` | 询问美食信息 |
| "故宫门票多少钱？" | `general_chat` | 询问门票价格 |
| "现在去三亚天气怎么样？" | `general_chat` | 询问天气，调用工具 |
| "把天数改成5天" | `modify_plan` | 修改现有计划 |
| "换成上海吧" | `modify_plan` | 修改目的地 |
| "西湖周边有什么景点？" | `general_chat` | 询问景点，调用工具 |
| "洛阳有什么值得去的地方？" | `general_chat` | 询问推荐景点 |

## 代码改动总结

### 修改的方法

1. **`_analyze_intent()`**
   - 从关键词匹配改为LLM智能识别
   - 添加降级策略（LLM失败时使用关键词匹配）
   - 更严格的规划意图判断

2. **`_handle_general_chat()`**
   - 完全重写，支持工具调用
   - 添加工具调用决策逻辑
   - 分别处理距离查询、POI搜索、天气查询

3. **`_handle_plan_modification()`**
   - 从返回固定文本改为智能修改
   - 提取原有计划参数
   - 分析修改意图并重新生成计划

### 新增的方法

1. **`_decide_tool_usage()`** - 判断是否需要调用工具
2. **`_handle_distance_query()`** - 处理距离查询
3. **`_handle_poi_search()`** - 处理POI搜索
4. **`_handle_weather_query()`** - 处理天气查询
5. **`_handle_tool_call()`** - 通用工具调用处理
6. **`_handle_llm_chat()`** - 使用LLM直接回答
7. **`_extract_trip_params_from_context()`** - 从上下文提取计划参数
8. **`_fallback_intent_analysis()`** - 降级的意图识别

## 使用效果

### 场景1：询问距离
**用户：** "龙门石窟和洛邑古城距离多远？"
**Agent：** 
- 识别为 `general_chat`
- 调用距离查询工具
- 返回距离信息
- ✅ 不会生成旅行计划

### 场景2：规划旅行
**用户：** "我想去北京玩3天"
**Agent：**
- 识别为 `plan_trip`
- 提取参数：城市=北京，天数=3
- 生成完整旅行计划
- ✅ 正确生成计划

### 场景3：修改计划
**用户：** "把天数改成5天"
**Agent：**
- 识别为 `modify_plan`
- 从历史中提取原计划参数
- 更新天数为5天
- 重新生成计划
- ✅ 正确修改并重新生成

### 场景4：询问信息
**用户：** "北京有什么好吃的？"
**Agent：**
- 识别为 `general_chat`
- 使用LLM回答美食推荐
- ✅ 不会生成旅行计划

## 注意事项

1. **LLM调用成本：** 意图识别和工具决策都需要调用LLM，会增加API调用次数
2. **降级策略：** 如果LLM调用失败，会自动降级到关键词匹配
3. **工具可用性：** 确保高德地图API配置正确，否则工具调用可能失败
4. **上下文依赖：** 修改计划功能依赖对话历史中有现有计划

## 后续优化建议

1. **缓存意图识别结果：** 对相似问题缓存识别结果，减少LLM调用
2. **完善工具调用：** 实现更多工具类型（路线规划、价格查询等）
3. **多轮对话优化：** 支持连续追问和上下文理解
4. **错误处理：** 更友好的错误提示和降级方案
